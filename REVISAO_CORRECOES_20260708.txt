REVISÃO DO SISTEMA - 08/07/2026

Correções aplicadas:
1. Corrigido erro no modal de recebimento/pagamento parcial:
   - adicionadas as funções maskMoneyInput(), maskPercentInput(), formatMoneyInput() e formatPercentInput().
   - antes o sistema chamava essas funções, mas elas não existiam, causando erro no JavaScript.

2. Melhorado o carregamento inicial do Dashboard:
   - renderAll() agora atualiza o Dashboard primeiro.
   - cada área da tela é renderizada com proteção contra erro isolado, evitando que um erro em Relatórios/Histórico bloqueie o Dashboard.
   - telas pesadas fora da aba ativa são carregadas de forma adiada para melhorar desempenho.

3. Melhorado o desempenho geral:
   - evitado renderizar tudo de forma pesada imediatamente em todas as trocas de aba.
   - mantida compatibilidade com todas as abas e funções atuais.

4. Corrigida duplicidade no salvamento da configuração tipoTaxaAtrasoDiario no localStorage.

5. Atualizado versionamento do app.js e do service worker:
   - evita que o navegador use arquivo antigo em cache.

Validação feita:
- Verificação de sintaxe JavaScript com node --check em todos os arquivos JS.
- Conferência dos principais handlers onclick/oninput usados no HTML e nos modais.

Observação:
- Não foi alterada a regra principal de cálculo financeiro.
- A estrutura visual original foi preservada.


AJUSTE NOVO VALLE - 2026-07-08 19:25
- Removidos os botões Salvar e imprimir e Salvar e enviar PDF da aba Novo VALLE.
- Mantidos somente os botões Cancelar e Salvar.
- Ao clicar no menu Novo, o formulário agora abre sempre limpo.
- Ao editar um vale pelo histórico, os dados do vale são preservados no formulário.
- Eventos antigos dos botões removidos ficaram protegidos para evitar erro de JavaScript.
